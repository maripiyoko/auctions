%h2 開催中のオークション一覧

%table.table
  %thead
    %tr
      %th オークション
      %th 出品者
      %th オークション締切日
      %th 最低入札価格
      %th 現在の入札数
      %th 現在の最高入札金額
  %tbody
    = render @auctions

= paginate @auctions
